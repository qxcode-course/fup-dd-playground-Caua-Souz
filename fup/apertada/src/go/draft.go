package main
import "fmt"
func main() {
    var n1, n2, n3, n4, n5 int
    fmt.Scan(&n1)
    fmt.Scan(&n2)
    fmt.Scan(&n3)
    fmt.Scan(&n4)
    fmt.Scan(&n5)

    lista := make([]int, {n1, n2, n3, n4, n5}


    fmt.Println("Hello, World!")
}
