package main
import "fmt"
import "bufio"
import "os"



func main() {
   s := bufio.NewScanner(os.Stdin)
   s.Scan()
    texto := s.Text()

    for i := 0 ; i < len(texto)/2 ; i++ {
        aux := texto[i]
        if texto
    }
}
