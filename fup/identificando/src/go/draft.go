package main
import "fmt"
import "bufio"
import "os"
import "strings"
import "unicode"
func main() {
    scanner := bufio.NewScanner(os.Stdin)
    scanner.Scan()
    linha := scanner.Split(scanner.Text(), "")
    var result []string

    for _, 
    fmt.Println("Hello, World!")
}